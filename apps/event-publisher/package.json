{
  "name": "@repo/pull-worker",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "drizzle-orm": "^0.44.4",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
